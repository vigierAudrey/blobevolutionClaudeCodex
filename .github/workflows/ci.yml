name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: '22'
  PNPM_VERSION: '10.28.2'

jobs:
  security-guard:
    name: Security Guard - DB Push Protection
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Ensure ripgrep is available
        run: |
          set -euo pipefail
          if command -v rg >/dev/null 2>&1; then
            echo "✅ ripgrep already available"
            rg --version
            exit 0
          fi

          if command -v apt-get >/dev/null 2>&1; then
            if command -v sudo >/dev/null 2>&1; then
              sudo apt-get update -y
              sudo apt-get install -y -qq ripgrep
            else
              apt-get update -y
              apt-get install -y -qq ripgrep
            fi
          else
            VERSION="14.1.0"
            TARGET="x86_64-unknown-linux-musl"
            ARCHIVE="ripgrep-${VERSION}-${TARGET}.tar.gz"
            URL="https://github.com/BurntSushi/ripgrep/releases/download/${VERSION}/${ARCHIVE}"
            TMPDIR="$(mktemp -d)"
            trap 'rm -rf "$TMPDIR"' EXIT
            mkdir -p "$HOME/.local/bin"
            if command -v curl >/dev/null 2>&1; then
              curl -fsSL "$URL" -o "$TMPDIR/$ARCHIVE"
            elif command -v wget >/dev/null 2>&1; then
              wget -qO "$TMPDIR/$ARCHIVE" "$URL"
            else
              echo "❌ Neither curl nor wget is available to install ripgrep." >&2
              exit 1
            fi
            tar -xzf "$TMPDIR/$ARCHIVE" -C "$TMPDIR"
            install -m 0755 "$TMPDIR/ripgrep-${VERSION}-${TARGET}/rg" "$HOME/.local/bin/rg"
            export PATH="$HOME/.local/bin:$PATH"
            if [ -n "${GITHUB_PATH:-}" ]; then
              echo "$HOME/.local/bin" >> "$GITHUB_PATH"
            fi
          fi

          rg --version

      - name: No skipped tests in critical API suites (block)
        run: bash scripts/no-skip-critical-check.sh

      - name: No insecure logs / secrets (block)
        run: |
          set -euo pipefail
          scan_runtime_files() {
            find apps/api/src apps/web -type f \
              ! -path '*/__tests__/*' \
              ! -path '*/test-utils/*' \
              ! -path '*/scripts/*' \
              ! -path '*/storybook-static/*' \
              ! -path '*/.next/*' \
              ! -path '*/node_modules/*' \
              ! -name '*.test.*' \
              ! -name '*.spec.*' \
              -print0
          }

          CONSOLE_HITS="$(
            scan_runtime_files \
              | xargs -0 -r grep -I -nE 'console\.(log|warn|error).*((process\.env)|(req\.(headers|cookies))|(Authorization)|(Bearer[[:space:]])|(accessToken[^[:alnum:]_])|(refreshToken[^[:alnum:]_]))' \
              || true
          )"
          LOGGER_HITS="$(
            scan_runtime_files \
              | xargs -0 -r grep -I -nE 'secureLogger\.(info|warn|error|security).*\{[^}]*\b(accessToken|refreshToken|authorization|password|token|secret)\s*:' \
              || true
          )"

          if [ -n "$CONSOLE_HITS" ] || [ -n "$LOGGER_HITS" ]; then
            echo "❌ Insecure logs / secrets detected in runtime source files."
            if [ -n "$CONSOLE_HITS" ]; then
              echo "$CONSOLE_HITS"
            fi
            if [ -n "$LOGGER_HITS" ]; then
              echo "$LOGGER_HITS"
            fi
            exit 1
          fi

          echo "✅ no-insecure-logs-check: no sensitive runtime log patterns detected."

      - name: No bare db push in CI paths (block)
        run: |
          set -euo pipefail
          list_ci_files() {
            if [ -d .github/workflows ]; then
              find .github/workflows -type f \( -name '*.yml' -o -name '*.yaml' \) -print0
            fi
            if [ -d .github/actions ]; then
              find .github/actions -type f \( -name '*.yml' -o -name '*.yaml' -o -name '*.sh' \) -print0
            fi
            if [ -d .gitlab ]; then
              find .gitlab -type f \( -name '*.yml' -o -name '*.yaml' -o -name '*.sh' \) -print0
            fi
            if [ -f .gitlab-ci.yml ]; then
              printf '%s\0' .gitlab-ci.yml
            fi
          }

          BLOCKED_SINGLE="$(
            list_ci_files \
              | xargs -0 -r grep -I -nE '^[[:space:]]*(run:[[:space:]]*)?(prisma[[:space:]]+db[[:space:]]+push|pnpm[[:space:]]+exec[[:space:]]+prisma[[:space:]]+db[[:space:]]+push|npx[[:space:]]+prisma[[:space:]]+db[[:space:]]+push|bash[[:space:]]+-lc[[:space:]]+["'\'']prisma[[:space:]]+db[[:space:]]+push|sh[[:space:]]+-c[[:space:]]+["'\'']prisma[[:space:]]+db[[:space:]]+push|pnpm[[:space:]]+dlx[[:space:]]+prisma[[:space:]]+db[[:space:]]+push|pnpm[[:space:]]+--filter[[:space:]]+@blobinfini/database[[:space:]]+db:push:unsafe)(["'\''])?([[:space:];|&]|$)' \
              || true
          )"

          BLOCKED_CONTINUATION_FILES="$(
            list_ci_files \
              | xargs -0 -r grep -I -l -E '^[[:space:]]*prisma[[:space:]]+db[[:space:]]*\\[[:space:]]*$' \
              || true
          )"
          BLOCKED_CONTINUATION=""
          if [ -n "$BLOCKED_CONTINUATION_FILES" ]; then
            while IFS= read -r file; do
              if grep -I -nE '^[[:space:]]*push([[:space:]]|$)' "$file" >/dev/null; then
                BLOCKED_CONTINUATION="${BLOCKED_CONTINUATION}${file}"$'\n'
              fi
            done < <(printf '%s\n' "$BLOCKED_CONTINUATION_FILES")
          fi

          if [ -n "$BLOCKED_SINGLE" ] || [ -n "$BLOCKED_CONTINUATION" ]; then
            echo "❌ Forbidden Prisma db push invocation detected in CI paths."
            if [ -n "$BLOCKED_SINGLE" ]; then
              echo "$BLOCKED_SINGLE"
            fi
            if [ -n "$BLOCKED_CONTINUATION" ]; then
              echo "$BLOCKED_CONTINUATION"
            fi
            exit 1
          fi

          echo "✅ ci-block-db-push: no forbidden db push invocation in CI paths."

      - name: Self-test ci-block-db-push (poison pill + variantes pnpm exec/npx/unsafe/continuation)
        # Prouve que le guard bloque les formes d'invocation de db push dans un workflow.
        # Chaque fichier poison est un workflow YAML valide (name/on/jobs/steps) pour tester
        # un scénario réaliste: le guard doit bloquer même dans une structure YAML bien formée.
        run: |
          set -euo pipefail
          guard_ci_db_push() {
            list_ci_files() {
              if [ -d .github/workflows ]; then
                find .github/workflows -type f \( -name '*.yml' -o -name '*.yaml' \) -print0
              fi
              if [ -d .github/actions ]; then
                find .github/actions -type f \( -name '*.yml' -o -name '*.yaml' -o -name '*.sh' \) -print0
              fi
              if [ -d .gitlab ]; then
                find .gitlab -type f \( -name '*.yml' -o -name '*.yaml' -o -name '*.sh' \) -print0
              fi
              if [ -f .gitlab-ci.yml ]; then
                printf '%s\0' .gitlab-ci.yml
              fi
            }

            local blocked_single blocked_continuation_files blocked_continuation
            blocked_single="$(
              list_ci_files \
                | xargs -0 -r grep -I -nE '^[[:space:]]*(run:[[:space:]]*)?(prisma[[:space:]]+db[[:space:]]+push|pnpm[[:space:]]+exec[[:space:]]+prisma[[:space:]]+db[[:space:]]+push|npx[[:space:]]+prisma[[:space:]]+db[[:space:]]+push|bash[[:space:]]+-lc[[:space:]]+["'\'']prisma[[:space:]]+db[[:space:]]+push|sh[[:space:]]+-c[[:space:]]+["'\'']prisma[[:space:]]+db[[:space:]]+push|pnpm[[:space:]]+dlx[[:space:]]+prisma[[:space:]]+db[[:space:]]+push|pnpm[[:space:]]+--filter[[:space:]]+@blobinfini/database[[:space:]]+db:push:unsafe)(["'\''])?([[:space:];|&]|$)' \
                || true
            )"
            blocked_continuation_files="$(
              list_ci_files \
                | xargs -0 -r grep -I -l -E '^[[:space:]]*prisma[[:space:]]+db[[:space:]]*\\[[:space:]]*$' \
                || true
            )"
            blocked_continuation=""
            if [ -n "$blocked_continuation_files" ]; then
              while IFS= read -r file; do
                if grep -I -nE '^[[:space:]]*push([[:space:]]|$)' "$file" >/dev/null; then
                  blocked_continuation="${blocked_continuation}${file}"$'\n'
                fi
              done < <(printf '%s\n' "$blocked_continuation_files")
            fi
            if [ -n "$blocked_single" ] || [ -n "$blocked_continuation" ]; then
              return 1
            fi
          }

          POISON=".github/workflows/_poison_test_$$.yml"
          trap 'rm -f "$POISON"' EXIT
          _poison_check() {
            local run_cmd="$1" label="$2"
            # Génère un workflow YAML valide contenant la commande interdite.
            printf 'name: Poison Pill\non: [push]\njobs:\n  poison:\n    runs-on: ubuntu-22.04\n    steps:\n      - name: Sneaky step\n        run: %s\n' \
              "$run_cmd" > "$POISON"
            local code=0
            guard_ci_db_push || code=$?
            rm -f "$POISON"
            if [ "$code" -eq 0 ]; then
              echo "❌ POISON FAILED [${label}]: ci-block-db-push.sh n'a pas bloqué"
              exit 1
            fi
            echo "✅ POISON OK [${label}]"
          }
          _poison_check "prisma db push"                                "naked"
          _poison_check "pnpm exec prisma db push"                      "pnpm-exec"
          _poison_check "npx prisma db push"                            "npx"
          _poison_check 'bash -lc "prisma db push"'                     "bash-lc"
          _poison_check 'sh -c "prisma db push"'                        "sh-c"
          _poison_check "pnpm dlx prisma db push"                       "pnpm-dlx"
          _poison_check "pnpm --filter @blobinfini/database db:push:unsafe" "db:push:unsafe"
          # Multi-line continuation bypass: guard must catch `prisma db \` at end of line
          printf 'name: Poison Pill\non: [push]\njobs:\n  poison:\n    runs-on: ubuntu-22.04\n    steps:\n      - name: Sneaky step\n        run: |\n          prisma db \\\n          push\n' \
            > "$POISON"
          code=0; guard_ci_db_push || code=$?
          rm -f "$POISON"
          if [ "$code" -eq 0 ]; then
            echo "❌ POISON FAILED [continuation]: multi-line db push not blocked"
            exit 1
          fi
          echo "✅ POISON OK [continuation]"

      - name: Enforce DB push guardrails
        run: |
          set -euo pipefail
          UNSAFE_HITS="$(
            find . -type f \
              \( -name '*.sh' -o -name '*.mjs' -o -name 'package.json' -o -name '*.yml' -o -name '*.yaml' \) \
              ! -path './.git/*' \
              ! -path './node_modules/*' \
              ! -path './apps/web/.next/*' \
              ! -path './apps/web/storybook-static/*' \
              ! -path './packages/database/scripts/safe-db-push.mjs' \
              ! -path './packages/database/package.json' \
              -print0 \
              | xargs -0 -r grep -I -nE 'accept-data-loss|db:push:unsafe' \
              || true
          )"
          if [ -n "$UNSAFE_HITS" ]; then
            echo "❌ Forbidden unsafe DB push guardrail bypass detected."
            echo "$UNSAFE_HITS"
            exit 1
          fi
          echo "✅ no-unsafe-db-push-check: no forbidden unsafe db push bypass outside approved local wrappers."

      - name: Verify Prisma supply-chain (single version in lockfile)
        # Checks pnpm-lock.yaml for unexpected prisma/@prisma/client versions.
        # pnpm.overrides in root package.json should enforce 6.19.0 everywhere.
        # Fails CI if any other version is resolved — no pnpm install required.
        run: |
          set -euo pipefail
          EXPECTED="6.19.0"
          BAD=$(grep -E "^\s*'?(prisma|@prisma/(client|config))@" pnpm-lock.yaml \
            | grep -oE "[0-9]+\.[0-9]+\.[0-9]+" \
            | sort -u \
            | grep -v "^${EXPECTED}$" || true)
          if [ -n "$BAD" ]; then
            echo "❌ Unexpected Prisma version(s) in pnpm-lock.yaml:"
            echo "$BAD"
            echo "   Expected exclusively: ${EXPECTED}"
            echo "   Check pnpm.overrides in root package.json."
            exit 1
          fi
          echo "✅ Prisma supply-chain: unique version ${EXPECTED} confirmed in pnpm-lock.yaml."

      - name: Verify ALLOW_ACCEPT_DATA_LOSS is not set in CI
        run: |
          echo "🔒 Checking CI environment for forbidden flags..."

          # Hard deny if ALLOW_ACCEPT_DATA_LOSS is set to true
          if [ "$ALLOW_ACCEPT_DATA_LOSS" = "true" ]; then
            echo "❌ CRITICAL SECURITY VIOLATION DETECTED!"
            echo "   ALLOW_ACCEPT_DATA_LOSS is set to 'true' in CI environment."
            echo ""
            echo "   This flag enables 'prisma db push --accept-data-loss' which can"
            echo "   result in irreversible data loss in production databases."
            echo ""
            echo "   IMMEDIATE ACTION REQUIRED:"
            echo "   - Remove ALLOW_ACCEPT_DATA_LOSS from workflow env vars"
            echo "   - Remove ALLOW_ACCEPT_DATA_LOSS from GitHub secrets"
            echo "   - Use 'prisma migrate deploy' for production deployments"
            echo ""
            exit 1
          fi

          # Check workflow/action files for accidental ALLOW_ACCEPT_DATA_LOSS=true assignments
          # (YAML env assignment or shell export only; ignore security guard comparisons)
          # Scans .github/workflows + .github/actions recursively via rg (--no-pcre2 = Rust regex,
          # supports \b word boundary and [[:space:]] POSIX classes; preferred over grep here).
          FORBIDDEN_PATTERN='^[[:space:]]*(ALLOW_ACCEPT_DATA_LOSS[[:space:]]*:[[:space:]]*("true"|'\'true\''|true)\b|export[[:space:]]+ALLOW_ACCEPT_DATA_LOSS[[:space:]]*=[[:space:]]*("true"|'\'true\''|true)\b|ALLOW_ACCEPT_DATA_LOSS[[:space:]]*=[[:space:]]*("true"|'\'true\''|true)\b)'
          ADLHITS="$(rg --no-pcre2 -rn --glob '*.yml' --glob '*.yaml' "$FORBIDDEN_PATTERN" .github/workflows .github/actions 2>/dev/null || true)"
          if [[ -n "$ADLHITS" ]]; then
            echo "❌ SECURITY VIOLATION: Found ALLOW_ACCEPT_DATA_LOSS=true in workflow files!"
            echo ""
            echo "   Detected in:"
            echo "$ADLHITS"
            echo ""
            echo "   This flag must NEVER be set in CI workflows."
            echo "   It is only permitted in local test environments."
            echo ""
            exit 1
          fi

          echo "✅ Security check passed: No forbidden flags detected."
          echo "   ALLOW_ACCEPT_DATA_LOSS: ${ALLOW_ACCEPT_DATA_LOSS:-not set}"

  lint:
    needs: security-guard
    name: Lint
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Enforce network guardrails
        run: |
          set -euo pipefail
          RAW_FETCH_HITS="$(
            find .github/workflows .github/actions scripts -type f \
              \( -name '*.yml' -o -name '*.yaml' -o -name '*.sh' \) \
              -print0 2>/dev/null \
              | xargs -0 -r grep -I -nE '((curl|wget)[^|]*\|[[:space:]]*(sudo[[:space:]]+)?(ba|z)?sh\b|bash[[:space:]]+<\([[:space:]]*(curl|wget)|sh[[:space:]]+<\([[:space:]]*(curl|wget))' \
              || true
          )"
          if [ -n "$RAW_FETCH_HITS" ]; then
            echo "❌ Raw remote fetch piped to a shell is forbidden."
            echo "$RAW_FETCH_HITS"
            exit 1
          fi
          echo "✅ no-raw-fetch-check: no curl|bash or wget|sh pattern detected in CI/shell scripts."

      - name: No insecure logs / secrets (block)
        run: |
          set -euo pipefail
          scan_runtime_files() {
            find apps/api/src apps/web -type f \
              ! -path '*/__tests__/*' \
              ! -path '*/test-utils/*' \
              ! -path '*/scripts/*' \
              ! -path '*/storybook-static/*' \
              ! -path '*/.next/*' \
              ! -path '*/node_modules/*' \
              ! -name '*.test.*' \
              ! -name '*.spec.*' \
              -print0
          }

          CONSOLE_HITS="$(
            scan_runtime_files \
              | xargs -0 -r grep -I -nE 'console\.(log|warn|error).*((process\.env)|(req\.(headers|cookies))|(Authorization)|(Bearer[[:space:]])|(accessToken[^[:alnum:]_])|(refreshToken[^[:alnum:]_]))' \
              || true
          )"
          LOGGER_HITS="$(
            scan_runtime_files \
              | xargs -0 -r grep -I -nE 'secureLogger\.(info|warn|error|security).*\{[^}]*\b(accessToken|refreshToken|authorization|password|token|secret)\s*:' \
              || true
          )"

          if [ -n "$CONSOLE_HITS" ] || [ -n "$LOGGER_HITS" ]; then
            echo "❌ Insecure logs / secrets detected in runtime source files."
            if [ -n "$CONSOLE_HITS" ]; then
              echo "$CONSOLE_HITS"
            fi
            if [ -n "$LOGGER_HITS" ]; then
              echo "$LOGGER_HITS"
            fi
            exit 1
          fi

          echo "✅ no-insecure-logs-check: no sensitive runtime log patterns detected."

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run lint (strict mode, zero warnings)
        run: pnpm run lint:strict

  build-and-test:
    name: Build and Test API
    runs-on: ubuntu-22.04
    needs: lint
    services:
      postgres:
        image: postgis/postgis:16-3.4
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: blobinfini
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      NODE_ENV: test
      NODE_OPTIONS: --max-old-space-size=4096
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/blobinfini
      SHADOW_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/blobinfini_shadow
      JWT_SECRET: test-jwt-secret
      JWT_REFRESH_SECRET: test-jwt-refresh-secret
      WEB_BASE_URL: http://localhost:3000
      NEXT_PUBLIC_API_URL: http://localhost:4000
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Wait for Postgres to be ready
        # Boucle explicite pg_isready (30s timeout) — ne pas supposer que le healthcheck
        # du service container suffit: il certifie l'état du conteneur, pas la disponibilité
        # TCP du port ni la capacité à accepter des requêtes applicatives.
        run: |
          set -euo pipefail
          sudo apt-get update -qq && sudo apt-get install -y -qq postgresql-client
          deadline=$((SECONDS + 30))
          until pg_isready -h localhost -U postgres -d blobinfini; do
            if [ $SECONDS -ge $deadline ]; then
              echo "❌ Postgres not ready after 30s — aborting." >&2
              exit 1
            fi
            sleep 1
          done
          echo "✅ Postgres TCP ready."
          # Verify applicative readiness: auth + correct DB via DATABASE_URL.
          # pg_isready only certifies TCP; this proves the credentials work.
          psql "$DATABASE_URL" -c "select 1" > /dev/null
          echo "✅ Postgres auth ready (DATABASE_URL)."

      - name: Canary DB clean replay
        run: pnpm --filter @blobinfini/database migrate:test:clean

      - name: Enable PostGIS
        env:
          PGPASSWORD: postgres
        run: |
          psql -h localhost -U postgres -d blobinfini -c "CREATE EXTENSION IF NOT EXISTS postgis;"

      - name: Generate Prisma Client
        run: pnpm run db:generate

      - name: Preflight DB migration state (allow pending before deploy)
        run: pnpm run db:preflight:allow-pending

      - name: Run DB migrations
        run: pnpm run db:migrate:deploy

      - name: Preflight DB schema lockstep (strict after deploy)
        run: pnpm run db:preflight

      - name: Verify DB constraints (EXCLUSION CONSTRAINT + UNIQUE INDEX)
        # Proves that migrate deploy produced the business-critical constraints
        # (not just that migrations ran — but that the actual schema objects exist).
        # Also activates the Jest globalSetup CI guard via JEST_DB_PREPARED=true below.
        run: node scripts/check-btree-gist.mjs --verify-constraints

      - name: Run build
        run: pnpm run build

      - name: Run API tests
        # JEST_DB_PREPARED=true: DB already migrated above — skip globalSetup migrate step
        # and activate the pg_constraint + pg_indexes guard in jest.global-setup.cjs.
        env:
          JEST_DB_PREPARED: 'true'
        run: pnpm --filter @blobinfini/api test

  api-e2e-db-migrations:
    name: "E2E DB Constraints (migrate deploy only)"
    runs-on: ubuntu-22.04
    needs: lint
    # BLOQUANT — échoue si migrate deploy ne produit pas les contraintes attendues.
    # Ce job prouve que les migrations 20260228200000 et 20260228200100
    # (EXCLUSION CONSTRAINT anti-overlap + UNIQUE INDEX one-per-day Europe/Paris)
    # sont appliquées via prisma migrate deploy et que btree_gist est disponible.
    services:
      postgres:
        image: postgis/postgis:16-3.4
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: blobinfini_e2e_constraints
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      CI: 'true'
      NODE_ENV: test
      NODE_OPTIONS: --max-old-space-size=4096
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/blobinfini_e2e_constraints
      # JEST_DB_PREPARED removed: replaced by jest.constraints.config.cjs which sets globalSetup: undefined statically.
      JWT_SECRET: test-jwt-secret
      JWT_REFRESH_SECRET: test-jwt-refresh-secret
      WEB_BASE_URL: http://localhost:3000
      NEXT_PUBLIC_API_URL: http://localhost:4000
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Wait for Postgres to be ready
        run: |
          set -euo pipefail
          sudo apt-get update -qq && sudo apt-get install -y -qq postgresql-client
          deadline=$((SECONDS + 30))
          until pg_isready -h localhost -U postgres -d blobinfini_e2e_constraints; do
            if [ $SECONDS -ge $deadline ]; then
              echo "❌ Postgres not ready after 30s — aborting." >&2
              exit 1
            fi
            sleep 1
          done
          echo "✅ Postgres TCP ready."
          psql "$DATABASE_URL" -c "select 1" > /dev/null
          echo "✅ Postgres auth ready (DATABASE_URL)."

      - name: "Preflight — ensure btree_gist is available and installable"
        run: node scripts/check-btree-gist.mjs

      - name: Generate Prisma Client
        run: pnpm run db:generate

      - name: "Run prisma migrate deploy on a fresh dedicated database"
        # RÈGLE: utiliser UNIQUEMENT migrate deploy ici, jamais db push.
        # La migration 20260228200000 installe btree_gist et crée l'EXCLUSION CONSTRAINT.
        # La migration 20260228200100 crée le UNIQUE INDEX one-per-day Europe/Paris.
        # Si ces contraintes étaient absentes (DB préparée par un autre moyen),
        # le step --verify-constraints qui suit détecterait la régression silencieuse.
        run: pnpm --filter @blobinfini/database exec prisma migrate deploy

      - name: "Drift strict — SQL direct (stable, sans parsing texte)"
        # Compare les dossiers prisma/migrations/ au contenu de _prisma_migrations via pg.
        # Échoue si: table absente, migration FS sans row finished_at, ou row avec rolled_back_at.
        # Indépendant du wording de la CLI Prisma — robuste aux upgrades de version.
        run: node scripts/check-btree-gist.mjs --verify-migration-drift

      - name: "Post-migration — vérifier EXCLUSION CONSTRAINT + UNIQUE INDEX présents"
        run: node scripts/check-btree-gist.mjs --verify-constraints

      - name: "Run booking.availability-constraints.e2e.test (DB constraints only)"
        # Test ciblé: prouve que les contraintes bloquent les races conditions au niveau DB.
        # Ces tests ÉCHOUENT si la DB a été préparée via db push au lieu de migrate deploy.
        # jest.constraints.config.cjs: globalSetup absent (DB déjà prête via migrate deploy ci-dessus).
        # --forceExit justifié: Prisma Client maintient des connexions internes qui bloquent sinon l'exit.
        run: |
          pnpm --filter @blobinfini/api exec jest \
            --config jest.constraints.config.cjs \
            --testPathPatterns booking.availability-constraints.e2e.test.ts \
            --no-coverage \
            --runInBand \
            --forceExit

  migrate-test-derived:
    name: Advisory Derived Migration Replay
    runs-on: ubuntu-22.04
    needs: lint
    continue-on-error: true
    services:
      postgres:
        image: postgis/postgis:16-3.4
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: blobinfini
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      NODE_ENV: test
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/blobinfini
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Wait for Postgres to be ready
        run: |
          set -euo pipefail
          sudo apt-get update -qq && sudo apt-get install -y -qq postgresql-client
          deadline=$((SECONDS + 30))
          until pg_isready -h localhost -U postgres -d blobinfini; do
            if [ $SECONDS -ge $deadline ]; then
              echo "❌ Postgres not ready after 30s — aborting." >&2
              exit 1
            fi
            sleep 1
          done
          echo "✅ Postgres ready."

      - name: Run advisory derived migration replay
        run: pnpm --filter @blobinfini/database migrate:test:derived

  type-check:
    name: Type Check
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Type check API
        run: pnpm --filter @blobinfini/api type-check

      - name: Type check Web
        continue-on-error: true
        run: pnpm --filter @blobinfini/web type-check
        # TODO: Fix web type-check (missing deps: zod, @storybook/*, unified, mdx)

  web-tests:
    name: Web Tests
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run web tests
        run: pnpm --filter @blobinfini/web test --detectOpenHandles

  contracts:
    name: API Contracts
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint OpenAPI specs
        run: pnpm run openapi:lint

  e2e-tests:
    name: E2E Tests
    runs-on: ubuntu-22.04
    if: github.event_name != 'pull_request' || github.base_ref == 'main'
    services:
      postgres:
        image: postgis/postgis:15-3.4
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres
        # Port mapping requis : job sans container — le runner accède au service via localhost.
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/blobinfini
      SHADOW_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/blobinfini_shadow
      SESSION_SECRET: ci-session-secret
      JWT_SECRET: ci-jwt-secret
      JWT_REFRESH_SECRET: ci-jwt-refresh-secret
      NEXT_PUBLIC_API_URL: http://127.0.0.1:4000
      WEB_BASE_URL: http://127.0.0.1:3002
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Prepare PostGIS (create DBs + extensions)
        run: |
          sudo apt-get update && sudo apt-get install -y postgresql-client
          export PGPASSWORD=postgres
          # Timeout augmenté à 60s : postgis/postgis:15-3.4 peut être lent à démarrer.
          deadline=$((SECONDS + 60))
          until pg_isready -h localhost -U postgres; do
            if [ $SECONDS -ge $deadline ]; then
              echo "❌ Postgres not ready after 60s — aborting." >&2
              exit 1
            fi
            sleep 2
          done
          echo "✅ Postgres TCP ready."
          psql "postgresql://postgres:postgres@localhost:5432/postgres" -c "select 1" > /dev/null
          echo "✅ Postgres auth ready."
          psql "postgresql://postgres:postgres@localhost:5432/postgres" -c "CREATE DATABASE blobinfini;"
          psql "postgresql://postgres:postgres@localhost:5432/postgres" -c "CREATE DATABASE blobinfini_shadow;"
          psql "postgresql://postgres:postgres@localhost:5432/blobinfini" -c "CREATE EXTENSION IF NOT EXISTS postgis;"
          psql "postgresql://postgres:postgres@localhost:5432/blobinfini_shadow" -c "CREATE EXTENSION IF NOT EXISTS postgis;"

      - name: Generate Prisma Client
        run: pnpm run db:generate

      - name: Run DB migrations
        run: pnpm run db:migrate:deploy

      - name: Seed database
        run: pnpm run db:reseed

      - name: Install Playwright browsers
        run: pnpm exec playwright install --with-deps

      - name: Run Playwright E2E tests
        run: pnpm run test:e2e

  verify-import-alias:
    runs-on: ubuntu-22.04
    needs: build-and-test
    if: always()
    continue-on-error: true
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      # Non-bloquant: vérifie que les alias import respectent la casse (App Router casse sensible sur Linux)
      - name: Verify imports alias (case-sensitive)
        run: |
          echo "Checking for incorrect case-sensitive imports in web workspace..."
          ! git grep -n "from ['\"]@/lib/apiclient['\"]" apps/web/app
