# 💬 Retour Codex – Statue Storybook après downgrade

## 🔍 Résumé
- **Objectif** : Faire tourner Storybook + tests visuels en Next.js 14.
- **Action menée** : Downgrade vers la stack suggérée (`@storybook/nextjs@8.0.10`, `storybook@8.0.10` côté CLI, scripts Storybook simplifiés).
- **Résultat** : `npm run build-storybook --workspace @blobinfini/web` réussit désormais (10.8 s, warnings taille fichiers mais pas bloquants).

## 📟 Logs clefs
```
@storybook/cli v8.0.10
...
info => Preview built (8.82 s)
info => Output directory: apps/web/storybook-static
```

## ⚠️ Problème restant
- `npm run storybook:test` échoue encore sur `test-storybook` car il attend un Storybook live à `http://127.0.0.1:6006`.
- Mon script `scripts/run-storybook-tests.sh` build + sert statiquement via `http-server`, puis lance `test-storybook --ci --failOnConsole --url http://127.0.0.1:6006`.
- Malgré le serveur statique, `test-storybook` tente quand même de lancer son propre Storybook dev (ou ne détecte pas l’instance) et d’où l’erreur initiale/broken build interrogeant le consent prompt.

## ❓ Questions pour toi
1. As-tu déjà fiabilisé `test-storybook` en mode statique (`--url` sur un build généré) ? Je n’ai pas trouvé de doc claire post-downgrade.
2. Faut-il repasser sur Chromatic/Playwright snapshots au lieu de `test-storybook` pour éviter le dev server ?
3. Souhaites-tu que je remonte aux mainteneurs vu l’absence d’API stable pour tests statiques ?

## ✅ État actuel
- Build local : ✅
- CI (story visual test) : ❌ script `storybook:test` à adapter
- OpenAPI lint : ✅

Merci pour ton aide pour le point (tests visuels) – je peux reroller le script ou basculer sur une autre solution si tu valides.

